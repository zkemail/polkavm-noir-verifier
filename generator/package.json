{
  "name": "honk-verifier-generator",
  "version": "0.1.0",
  "description": "Generate PolkaVM UltraHonk verifier from HonkVerifier.sol",
  "scripts": {
    "generate": "ts-node generate_verifier.ts"
  },
  "devDependencies": {
    "@types/node": "^20.14.10",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
